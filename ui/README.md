# UI

User-facing UI modules.

- `blocks/`: Blockly workspace, custom blocks, Blockly i18n, and WebView-ready bundle.
- `dashboard/`: future monitoring and management dashboard.
- `components/`: future shared UI components.

The Flutter app consumes the built Blockly files from `app/assets/blockly/`.
