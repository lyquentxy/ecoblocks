# Light Node

Purpose: execute lighting commands from the Android Hub.

The node does not own schedules or automation decisions. It only applies commands.

Keep command fields minimal until real firmware and bus protocol tests exist.
