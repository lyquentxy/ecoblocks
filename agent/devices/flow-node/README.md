# Flow Node

Purpose: collect flow-like readings or execute simple flow-related actions under Hub control.

The node remains dumb. The Android Hub decides what the readings mean.

Keep payloads mock-minimal until hardware tests settle the protocol.
