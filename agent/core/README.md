# Core

Pure Dart package shared by the Flutter app.

Contains:

- Bridge message models.
- Mock device models.
- Agent runtime interfaces and mock/LLM-backed agents.

This package must not depend on Flutter.
