name: ecoblocks_core
description: EcoBlocks pure Dart core models, bridge messages, and agent runtime.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.2

dev_dependencies:
  test: ^1.24.0
